# example

Runnable Flutter app for **one_request**.

```bash
cd example
flutter pub get
flutter run
```

Buttons cover `request` / `send` / cache / batch / upload, WebSocket connect-send-close, and connectivity UI (off, snackbar, popover, banner, watch-only, custom). Nothing is forced on except HTTP demo calls.
